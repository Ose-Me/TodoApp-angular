import { NgModule } from '@angular/core';
import { COMPONENTS, UserRoutingModule } from './user-routing.module';

@NgModule({
  declarations: [],
  imports: [UserRoutingModule],
})
export class UserModule {}
